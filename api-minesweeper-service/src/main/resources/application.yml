server:
  servlet:
    context-path: /api/minesweeper
  port: 8080
management:
  endpoints:
    enabled-by-default: true
    web:
      base-path: /
    endpoint:
      health:
        show-details: always
spring:
  data:
    mongodb:
      uri: mongodb+srv://application:foobar@minescluster.jw2wr.mongodb.net/minesweeper?retryWrites=true&w=majority
